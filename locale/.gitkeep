# Project message catalogues live here: locale/<lang>/LC_MESSAGES/django.po (+ djangojs.po).
# Populated by `manage.py makemessages`; compiled `.mo` files are git-ignored (built in the
# image / CI). LOCALE_PATHS points here. See docs/i18n/design/11-catalogue-maintenance.md.
