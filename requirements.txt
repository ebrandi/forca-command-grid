# [FORCA] Command Grid — runtime dependencies
# Lower bounds are pinned to current security-patched releases so a prod build
# can't drift DOWN onto a known-vulnerable point release. Run `pip-audit` in CI.
Django>=5.2.15,<5.3          # 5.2.15 = latest security release (2026-06-03)
djangorestframework>=3.16,<3.17
drf-spectacular>=0.28,<0.29
celery>=5.5,<5.6
redis>=5.3,<6.0
psycopg[binary]>=3.2,<4.0
django-environ>=0.12,<0.13
gunicorn>=23.0,<24.0          # 23.0 fixes HTTP request smuggling (CVE-2024-6827)
whitenoise>=6.9,<7.0
# Hardened XML parsing for the EVE-client fitting import (XXE / entity-expansion
# / external-reference defence). Pure-Python, no C extension.
defusedxml>=0.7,<0.8
# 48.0.1 is the first release clear of the 44.0.0 advisories (CVE-2026-26007,
# GHSA-537c-gmf6-5ccf, PYSEC-2026-35, PYSEC-2026-1284).
cryptography>=49.0.0
# 2.13 is the first release clear of the 2.10.0 advisories (PYSEC-2026-175..179).
PyJWT>=2.13,<3.0
requests>=2.34,<3.0
# Transitive but security-critical — pin a safe floor explicitly.
# 2.7 is the first release clear of the 2.5.0 advisories (PYSEC-2026-141/1994/1996/1998).
urllib3>=2.7,<3.0
certifi>=2025.1.31
# Powers the scheduled dependency-vulnerability scan (admin_audit.audit_dependencies,
# weekly via Celery beat) so a newly-disclosed CVE in an installed package surfaces
# on-site without waiting for a manual review. Also runnable in CI / via
# `manage.py audit_dependencies`.
pip-audit>=2.10.1,<3.0
